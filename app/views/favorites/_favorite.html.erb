 <% if post.favorited_by?(current_user) %>
      <%= link_to post_favorites_path(post), class: 'btn btn-outline-info btn-lg rounded-pill', remote: true, method: :delete do %>
       ♥<%= post.favorites.count %> いいね
    <% end %>
    <% else %>
       <%= link_to post_favorites_path(post), class: 'btn btn-outline-info btn-lg rounded-pill', remote: true, method: :post do %>
       ♡<%= post.favorites.count %> いいね
    <% end %>
 <% end %>