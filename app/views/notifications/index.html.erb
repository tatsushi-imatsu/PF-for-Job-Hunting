<div class='row'>
   <div class="picture" style="width:100%">
    <div class="mx-auto" style="width:70%">
      <div class="box"><br>
        <h3 class="text-center">
          通知 〜Notification〜
        </h3>
        <% if @notifications.exists? %>
         <%= render @notifications %>
         <%= paginate @notifications %><br>
        <% else %>
         <p>通知はありません</p>
        <% end %>
      </div>
    </div>
   </div>
</div>