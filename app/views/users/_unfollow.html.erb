<%= form_for (url:relationships_path, method: :delete, remote: true) do |f| %>
   <%= f.hidden_field :followed_id, value: @user.id %>
   <%= f.submit "フォロー外す", class: 'btn btn-outline-light btn-lg rounded-pill' %>
   <% end %>