<%= form_for (url:relationships_path, remote: true) do |f| %>
   <%= f.hidden_field :followed_id, value: @user.id %>
   <%= f.submit "フォローする", class: 'btn btn-outline-light btn-lg rounded-pill' %>
  <% end %>